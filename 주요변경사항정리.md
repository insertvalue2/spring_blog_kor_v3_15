# 강의 진행 가이드 — 이메일 인증 처음부터 끝까지

이 문서는 강의를 진행하면서 그대로 따라갈 수 있도록 **순서대로** 정리한 가이드입니다. 각 단계마다 "목적 → 파일 → 코드 → 강의 포인트" 4가지 구조로 되어 있습니다.

---

## 전체 흐름 한눈에

| Part | 단계 | 목표 |
| --- | --- | --- |
| 사전 준비 | 0 | Gmail 앱 비밀번호 발급 |
| Part 1 | 1~7 | 이메일 인증 기본 기능 구축 |
| Part 2 | 8~11 | 이메일 중복 가입 방지 |
| Part 3 | 12~14 | 이메일 인증 우회 차단 (보안 강화) |

각 Part 가 끝났을 때 학생이 만들어 낸 상태를 한 줄로 정리하면 다음과 같습니다.

- Part 1 끝: 이메일 발송/확인은 되지만 **인증 없이도 가입 가능** (문제 있음)
- Part 2 끝: 같은 이메일로 두 번 가입 안 됨 (하지만 인증 우회는 여전히 가능)
- Part 3 끝: **서버에서 진짜 자물쇠 채움** (보안 완성)

---

## 사전 준비 — Gmail 앱 비밀번호 발급

### 0단계. 발신용 Gmail 계정에서 앱 비밀번호 발급

**목적**

우리 서버가 Gmail SMTP 로 메일을 보내려면 인증이 필요한데, 일반 로그인 비밀번호는 보안상 사용 불가. Google 이 별도로 발급해주는 16자리 "앱 비밀번호" 를 사용한다.

**순서**

1. Google 계정 ("https://myaccount.google.com") 로그인
2. "보안" 메뉴 → "2단계 인증" 활성화 (필수 선행 조건)
3. "보안" → "앱 비밀번호" → 새 앱 비밀번호 생성
4. 발급된 16자리 비밀번호 복사 (공백 없이, 영문/숫자 혼합)

**강의 포인트**

- "일반 비번 아니다" 를 한 번 못 박아주기. 학생들이 가장 헷갈리는 부분.
- 2단계 인증 미설정 계정은 앱 비번 메뉴 자체가 안 보임. 미리 안내.
- 발급된 키는 한 번만 보임. 분실 시 새로 발급.

> **비유**: Gmail 본인 비밀번호는 집 현관 열쇠. 앱 비밀번호는 "외부 업체에 줄 수 있는 임시 카드키" 같은 것. 분실해도 본 열쇠는 안전.

---

## Part 1 — 이메일 인증 기본 기능 구축

### 1단계. 메일 라이브러리 의존성 추가

**목적**

Spring Boot 의 메일 발송 도구를 사용할 수 있게 한다.

**파일**

"build.gradle"

**코드** (dependencies 블록에 한 줄 추가)

```gradle
dependencies {
    // ... 기존 의존성들 ...

    // 이메일 발송 라이브러리 (회원가입 이메일 인증용)
    implementation 'org.springframework.boot:spring-boot-starter-mail'

    // ... 나머지 ...
}
```

**강의 포인트**

- 추가 후 Gradle 새로고침 ("Reload Gradle Project") 잊지 말기
- "spring-boot-starter-mail" 한 줄로 JavaMailSender 빈이 자동 등록됨

---

### 2단계. SMTP 설정 추가

**목적**

의존성만 추가하면 안 되고, "어떤 메일 서버를 쓸지 + 어떤 계정으로 인증할지" 를 application.yml 에 알려준다.

**파일**

"src/main/resources/application-dev.yml"

**코드** (spring 블록 안에 추가)

```yaml
spring:
  # ... 기존 mustache, datasource, jpa 등 ...

  # ==========================================================
  # 메일 발송 SMTP 설정 (회원가입 이메일 인증용)
  # ==========================================================
  #  - host:     Gmail SMTP 서버 (고정)
  #  - port 587: TLS 암호화 통신 전용 포트
  #  - username: 본인의 전체 Gmail 주소로 변경
  #  - password: 0단계에서 발급받은 16자리 앱 비밀번호
  mail:
    host: smtp.gmail.com
    port: 587
    username: 본인_gmail_주소@gmail.com
    password: xxxxxxxxxxxxxxxx
    properties:
      mail:
        smtp:
          auth: true               # SMTP 인증 활성화
          starttls:
            enable: true           # TLS 보안 연결 활성화
            required: true         # TLS 강제 (보안상 true 권장)
```

**강의 포인트**

- 비밀번호 자리에 일반 비번 적으면 인증 실패. 반드시 앱 비밀번호.
- 587 포트는 TLS 용. 25 (평문), 465 (SSL) 와 헷갈리지 말 것.

---

### 3단계. 인증번호 생성 유틸 만들기

**목적**

6자리 랜덤 숫자를 생성하는 작은 헬퍼 함수.

**파일**

"src/main/java/com/tenco/blog/_core/util/MailUtils.java" (신규)

**코드**

```java
package com.tenco.blog._core.util;

import java.util.Random;

public class MailUtils {

    /**
     * 6자리 랜덤 숫자 문자열을 생성한다. 예) "123456"
     * 100_000 부터 시작하는 이유: 앞에 0 이 붙으면 6자리가 안 되므로.
     */
    public static String generateRandomCode() {
        Random random = new Random();
        int code = 100_000 + random.nextInt(900_000);
        return String.valueOf(code);
    }
}
```

**강의 포인트**

- 유틸 클래스는 정적 메서드 모음. 인스턴스 만들지 않고 "MailUtils.generateRandomCode()" 로 직접 호출.

---

### 4단계. EmailCheckDTO 추가

**목적**

AJAX 로 들어오는 "이메일 + 인증번호" 데이터를 받을 그릇.

**파일**

"src/main/java/com/tenco/blog/user/UserRequest.java" (기존 파일에 내부 클래스 추가)

**코드** (UserRequest 클래스 안에 추가)

```java
/**
 * 이메일 인증용 DTO
 * - 발송 요청: email 만 사용 (code 는 null)
 * - 확인 요청: email + code 둘 다 사용
 */
@Data
public static class EmailCheckDTO {
    private String email;
    private String code;

    public void validate() {
        if (email == null || email.trim().isEmpty()) {
            throw new IllegalArgumentException("이메일을 입력해주세요");
        }
        if (!email.contains("@")) {
            throw new IllegalArgumentException("올바른 이메일 형식이 아닙니다");
        }
    }
}
```

**강의 포인트**

- 같은 DTO 가 두 가지 용도로 쓰임. 발송 시엔 code 가 null.

---

### 5단계. MailService 구현 — 인증번호 발송 + 확인

**목적**

실제 메일을 보내고, 발송한 인증번호를 서버 세션에 기억해 두었다가 사용자가 입력한 값과 비교한다.

**파일**

"src/main/java/com/tenco/blog/user/MailService.java" (신규)

**코드** (Part 3 에서 한 줄만 추가됨 — 일단 기본 버전부터)

```java
@Slf4j
@RequiredArgsConstructor
@Service
public class MailService {

    private final JavaMailSender javaMailSender;
    private final HttpSession session;

    // 인증번호 발송
    public void 인증번호발송(String email) {
        String code = MailUtils.generateRandomCode();
        log.info("생성된 인증번호: {}", code);

        MimeMessage message = javaMailSender.createMimeMessage();
        try {
            MimeMessageHelper helper = new MimeMessageHelper(message, true, "UTF-8");
            helper.setTo(email);
            helper.setSubject("[MyBlog] 회원가입 이메일 인증번호");
            helper.setText("<h3>인증번호는 [" + code + "] 입니다.</h3>", true);

            javaMailSender.send(message);

            // 세션 저장 — 키 패턴: "code_" + 이메일 (사용자별 격리)
            session.setAttribute("code_" + email, code);
        } catch (MessagingException e) {
            throw new IllegalArgumentException("메일 발송에 실패했습니다: " + e.getMessage());
        }
    }

    // 인증번호 확인 (Part 3 에서 한 줄 추가 예정)
    public boolean 인증번호확인(String email, String code) {
        String savedCode = (String) session.getAttribute("code_" + email);

        if (savedCode != null && savedCode.equals(code)) {
            session.removeAttribute("code_" + email); // 일회용
            return true;
        }
        return false;
    }
}
```

**강의 포인트**

- 세션 키 패턴 "code_" + email — 동시 접속자가 많아도 사용자별로 격리됨.
- HttpSession 을 Service 에 주입하는 건 학습용 단순화. 실무는 Redis.
- 이 시점에서는 "인증 성공 후 그 사실을 어디에 기록하지 않는다". Part 3 에서 도장 추가.

> **비유**: 세션은 직원 머릿속의 임시 메모장. "이 손님이 받아간 번호는 이거" 라고 적어두고, 손님이 다시 와서 번호 부르면 비교.

---

### 6단계. UserApiController — AJAX 엔드포인트

**목적**

브라우저의 AJAX 가 호출할 두 개의 REST 엔드포인트.

**파일**

"src/main/java/com/tenco/blog/user/UserApiController.java" (신규)

**코드**

```java
@RequiredArgsConstructor
@RestController
public class UserApiController {

    private final MailService mailService;

    @PostMapping("/api/email/send")
    public ResponseEntity<?> 인증번호발송(@RequestBody UserRequest.EmailCheckDTO reqDTO) {
        reqDTO.validate();
        mailService.인증번호발송(reqDTO.getEmail());
        return ResponseEntity.ok().body(Map.of("message", "인증번호가 발송되었습니다."));
    }

    @PostMapping("/api/email/verify")
    public ResponseEntity<?> 인증번호확인(@RequestBody UserRequest.EmailCheckDTO reqDTO) {
        reqDTO.validate();
        if (reqDTO.getCode() == null || reqDTO.getCode().trim().isEmpty()) {
            return ResponseEntity.badRequest().body(Map.of("message", "인증번호를 입력해주세요."));
        }

        boolean isVerified = mailService.인증번호확인(reqDTO.getEmail(), reqDTO.getCode());

        if (isVerified) {
            return ResponseEntity.ok().body(Map.of("message", "인증되었습니다."));
        } else {
            return ResponseEntity.badRequest().body(Map.of("message", "인증번호가 일치하지 않습니다."));
        }
    }
}
```

**강의 포인트**

- "@RestController" = "@Controller" + "@ResponseBody". 화면 안 반환하고 JSON 만 응답.
- "@RequestBody" — AJAX 가 보낸 JSON 을 자동으로 DTO 로 변환.

---

### 7단계. join-form.mustache — UI + AJAX

**목적**

회원가입 폼 화면에 이메일 인증 UI 와 AJAX 호출 JavaScript 추가.

**파일**

"src/main/resources/templates/user/join-form.mustache"

**코드 — HTML 변경 핵심**

```html
<form action="/join" method="post" enctype="multipart/form-data" onsubmit="return checkVerify()">
    <!-- 사용자명 / 비밀번호 입력 (기존) -->

    <!-- 이메일 입력 + 인증번호 발송 버튼 -->
    <div class="mb-3">
        <label class="form-label">이메일</label>
        <div class="input-group">
            <input type="email" class="form-control" id="email" name="email"
                   placeholder="이메일 입력" required>
            <button type="button" class="btn btn-outline-secondary"
                    onclick="callSendApi()">인증번호 전송</button>
        </div>
    </div>

    <!-- 인증번호 입력 (처음엔 숨김, 발송 후 보임) -->
    <div class="mb-3" id="code-box" style="display:none;">
        <label class="form-label">인증번호</label>
        <div class="input-group">
            <input type="text" class="form-control" id="code"
                   placeholder="인증번호 6자리" maxlength="6">
            <button type="button" class="btn btn-outline-secondary"
                    onclick="callVerifyApi()">확인</button>
        </div>
        <div id="msg" class="form-text"></div>
    </div>

    <!-- 인증 완료 여부 저장 (hidden 필드) -->
    <input type="hidden" id="isEmailVerified" value="false">

    <!-- 프로필 이미지 (선택) / 회원가입 버튼 (기존) -->
</form>
```

**코드 — 핵심 JavaScript 3개**

```javascript
// (1) 인증번호 발송
async function callSendApi() {
    let email = document.querySelector("#email").value;
    if (!email) { alert("이메일을 입력해주세요."); return; }

    let response = await fetch("/api/email/send", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email: email })
    });

    if (response.ok) {
        alert("인증번호가 전송되었습니다!");
        document.querySelector("#code-box").style.display = "block";
    }
}

// (2) 인증번호 확인
async function callVerifyApi() {
    let email = document.querySelector("#email").value;
    let code = document.querySelector("#code").value;

    let response = await fetch("/api/email/verify", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email: email, code: code })
    });

    if (response.ok) {
        document.querySelector("#msg").innerHTML =
            "<span style='color:green'>인증되었습니다.</span>";
        document.querySelector("#isEmailVerified").value = "true";
        document.querySelector("#email").readOnly = true;
    } else {
        document.querySelector("#msg").innerHTML =
            "<span style='color:red'>인증번호가 틀렸습니다.</span>";
    }
}

// (3) 회원가입 제출 직전 최종 체크 (UX용)
function checkVerify() {
    let isVerified = document.querySelector("#isEmailVerified").value;
    if (isVerified !== "true") {
        alert("이메일 인증을 먼저 완료해주세요.");
        return false;
    }
    return true;
}
```

**강의 포인트**

- "fetch + await" — 페이지 새로고침 없이 서버와 데이터 주고받기.
- 인증 완료 후 이메일 입력란을 "readOnly" 로 잠가서 사용자가 인증 후 이메일을 바꾸지 못하게.

### Part 1 마무리 시연

학생들에게 보여줄 시연:

1. 폼에 이메일 입력 → "인증번호 전송" → 메일함 확인 → 6자리 수신
2. 인증번호 입력 → "확인" → "인증되었습니다"
3. 회원가입 제출 → DB 에 사용자 저장

**하지만 여기서 문제 시연**

> "선생님, 인증 안 누르고 가입 버튼 바로 누르면?"
> → alert "이메일 인증을 먼저 완료해주세요" (JS 가 막음, 정상)
>
> "개발자 도구로 hidden 필드를 true 로 바꾸면?"
> → **가입이 그냥 통과되어 버림** (충격적 결과)

이 시점에서 학생들 "어!?" 반응. 다음 Part 들로 자연스럽게 연결.

---

## Part 2 — 이메일 중복 가입 방지

### 8단계. UserRepository 에 findByEmail 추가

**목적**

"이 이메일이 이미 가입되어 있는지" 조회 가능하게.

**파일**

"src/main/java/com/tenco/blog/user/UserRepository.java"

**코드** (인터페이스 안에 추가)

```java
/**
 * 이메일로 사용자 조회 (이메일 중복 체크용)
 */
@Query("""
    SELECT u FROM User u WHERE u.email = :email
""")
Optional<User> findByEmail(@Param("email") String email);
```

**강의 포인트**

- 쿼리 메서드 네이밍 규칙 또는 @Query JPQL 둘 다 가능. 이 프로젝트는 @Query 스타일 통일.
- "Optional 로 반환" → 없으면 빈 Optional, 있으면 User 가 담긴 Optional.

---

### 9단계. User 엔티티의 email 에 유니크 제약 추가

**목적**

DB 레벨에서도 동일 이메일 중복 방지. 애플리케이션 로직과 함께 이중 방어.

**파일**

"src/main/java/com/tenco/blog/user/User.java"

**코드 변경**

```java
private String password;

// 이메일 유니크 제약 — 동일 이메일 중복 가입 방지 (DB 레벨 안전망)
@Column(unique = true)
private String email;
```

**강의 포인트**

- 애플리케이션 체크와 DB 제약, 왜 둘 다 필요한가?
    - 애플리케이션 체크: 친절한 에러 메시지를 위함
    - DB 제약: 거의 동시에 같은 이메일로 가입 누르는 경쟁 상황 방어
- "심층 방어 (Defense in Depth)" 라는 보안 원칙 한 줄 짚어주기.

---

### 10단계. UserService.회원가입 에 이메일 중복 체크 추가

**목적**

가입 진입 시점에 이메일 중복 확인.

**파일**

"src/main/java/com/tenco/blog/user/UserService.java"

**코드 변경** (사용자명 중복 체크 다음에 추가)

```java
// 이메일 중복 체크 (애플리케이션 레벨 친절한 에러 메시지용)
userRepository.findByEmail(joinDTO.getEmail()).ifPresent(user -> {
    log.warn("회원가입 실패 - 중복된 이메일 : {}", user.getEmail());
    throw new Exception400("이미 등록된 이메일입니다");
});
```

**강의 포인트**

- "ifPresent()" → Optional 의 값이 존재하면 람다 실행.
- Exception400 을 던지면 GlobalExceptionHandler 가 잡아서 alert 로 보여줌.

---

### 11단계. GlobalExceptionHandler — Exception400 alert + DB 제약 핸들러

**목적**

Exception400 을 alert 창으로 표시 (사용자 친화적). 동시 가입 같은 DB 레벨 위반도 친절히 응답.

**파일**

"src/main/java/com/tenco/blog/_core/errors/GlobalExceptionHandler.java"

**코드 — Exception400 핸들러 (기존 페이지 이동 → alert 으로 변경)**

```java
@ExceptionHandler(Exception400.class)
@ResponseBody
public String ex400(Exception400 e, HttpServletRequest request) {
    log.warn("=== 400 Bad Request 에러 발생 ===");
    log.warn("요청 URL: {}", request.getRequestURL());
    log.warn("에러메시지: {}", e.getMessage());

    String message = e.getMessage() != null ? e.getMessage() : "잘못된 요청입니다";
    String escapedMessage = message.replace("'", "\\'");
    return """
        <script>
            alert('%s');
            history.back();
        </script>
        """.formatted(escapedMessage);
}
```

**코드 — DB 제약 위반 핸들러 (친절 메시지 분기 추가)**

```java
@ExceptionHandler(DataIntegrityViolationException.class)
@ResponseBody
public String handleDataIntegrityViolationException(DataIntegrityViolationException e,
                                                    HttpServletRequest request) {
    String errorMessage = e.getMessage();
    String userMessage;

    if (errorMessage != null && errorMessage.contains("email")) {
        userMessage = "이미 등록된 이메일입니다";
    } else if (errorMessage != null && errorMessage.contains("username")) {
        userMessage = "이미 존재하는 사용자 이름입니다";
    } else if (errorMessage != null && errorMessage.contains("FOREIGN KEY")) {
        userMessage = "관련된 데이터가 있어 삭제할 수 없습니다";
    } else {
        userMessage = "데이터베이스 제약조건 위반이 발생했습니다";
    }

    String escapedMessage = userMessage.replace("'", "\\'");
    return """
        <script>
            alert('%s');
            history.back();
        </script>
        """.formatted(escapedMessage);
}
```

**강의 포인트**

- "alert" + "history.back()" 조합 — 사용자가 에러 확인 후 이전 화면으로 돌아가 다시 입력.
- 작은따옴표 이스케이프 — 에러 메시지에 따옴표가 있으면 JS 구문 오류가 나므로 "\\'" 로 변환.

### Part 2 마무리 시연

- 같은 이메일로 두 번 가입 → "이미 등록된 이메일입니다" alert
- 동시에 두 탭에서 같은 이메일로 가입 → 둘 중 하나는 DB 제약 위반으로 alert

> **하지만 여전히 문제**: "인증 안 해도 가입은 가능" 이라는 본질적 문제는 그대로. Part 3 로 이어짐.

---

## Part 3 — 이메일 인증 우회 차단 (이 단원의 핵심)

이 Part 의 핵심 메시지는 한 줄로 요약됩니다.

> **"클라이언트는 친절한 안내, 서버는 진짜 자물쇠."**
>
> 정상 사용자에게 JS 가 친절히 안내하지만, 진짜 보안은 서버에서 한다. 인증된 사실을 세션에 도장으로 남기고, 한 번 쓰면 제거한다.

### 12단계. MailService.인증번호확인 — "verified_email" 도장 찍기

**목적**

인증이 성공한 사실을 서버 세션에 기록해 둠.

**파일**

"src/main/java/com/tenco/blog/user/MailService.java"

**코드 변경** (인증번호확인 메서드 안)

```java
public boolean 인증번호확인(String email, String code) {
    String savedCode = (String) session.getAttribute("code_" + email);

    if (savedCode != null && savedCode.equals(code)) {
        // (1) 코드 일회용 처리 (기존)
        session.removeAttribute("code_" + email);

        // (2) [추가] "이 이메일은 인증 완료됨" 도장 찍기
        //   - 회원가입 시점에 UserService 가 확인할 도장
        //   - 브라우저가 아닌 서버 세션에 기록되므로 우회 불가
        session.setAttribute("verified_email", email);

        return true;
    }
    return false;
}
```

**강의 포인트**

- 한 줄 추가가 핵심: "session.setAttribute("verified_email", email)"
- 이 도장은 같은 브라우저(같은 세션) 안에서만 유효. 사용자별 자동 격리.

---

### 13단계. UserService — HttpSession 주입 + 도장 검사 + 사용 후 제거

**목적**

서버 진입 시점에서 인증 완료 여부 확인. 가입 성공 후엔 도장 제거(재사용 방지).

**파일**

"src/main/java/com/tenco/blog/user/UserService.java"

**코드 변경 — 필드 주입**

```java
import jakarta.servlet.http.HttpSession;

@Service
@RequiredArgsConstructor
public class UserService {

    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;

    // [추가] 세션 — verified_email 도장 확인용
    private final HttpSession session;

    // ...
}
```

**코드 변경 — 회원가입 진입 직후 도장 검사 + 가입 후 도장 제거**

```java
@Transactional
public User 회원가입(UserRequest.JoinDTO joinDTO) {
    log.info("회원가입 서비스 시작");

    // [핵심] 이메일 인증 도장 확인
    //
    // 클라이언트 측 검증은 우회 가능 (개발자 도구 변조, JS off, curl 직접 호출).
    // 따라서 진짜 검증은 서버 세션의 도장으로 한다.
    String verifiedEmail = (String) session.getAttribute("verified_email");
    if (verifiedEmail == null || !verifiedEmail.equals(joinDTO.getEmail())) {
        throw new Exception400("이메일 인증을 먼저 완료해주세요");
    }

    // (기존) 사용자명 중복 체크
    // (기존) 이메일 중복 체크
    // (기존) 프로필 이미지 저장
    // (기존) 비밀번호 암호화
    // ... 생략 ...

    User savedUser = userRepository.save(user);

    // [추가] 이메일 인증 도장 제거 (일회용 — 재사용 방지)
    session.removeAttribute("verified_email");

    return savedUser;
}
```

**강의 포인트**

- 검사 위치는 **유효성 검사 다음, 중복 체크 전**. 이메일 인증 안 했으면 다른 검증 더 할 필요 없으니까.
- 도장 검사는 두 가지 조건 모두 만족해야 통과: (1) 도장이 있어야 하고, (2) 도장의 이메일과 가입 폼 이메일이 일치해야 함.
- 가입 성공 후 도장 제거가 핵심 — 같은 도장으로 또 다른 계정 만드는 것 방지.

---

### 14단계. join-form.mustache — JS 검증의 역할 주석으로 명시

**목적**

학생/후속 개발자가 코드를 봤을 때 "JS 검증은 보안이 아니라 UX 용" 임을 명확히 알 수 있도록.

**파일**

"src/main/resources/templates/user/join-form.mustache"

**코드 변경 — checkVerify 주석 강화**

```javascript
// 회원가입 폼 제출 시 최종 체크
//
// [중요] 이 클라이언트 측 체크는 보안용이 아니라 UX 용이다.
//  - 정상 사용자가 인증 안 누른 채 가입 버튼만 누르는 실수를 미리 잡아 alert 띄움.
//  - 그러나 개발자 도구로 hidden 값을 바꾸거나 JS 를 끄면 우회 가능.
//  - 진짜 검증은 서버(UserService.회원가입) 의 "verified_email" 세션 도장이 한다.
//  - 우회해서 제출해도 서버에서 "이메일 인증을 먼저 완료해주세요" 로 거부됨.
//
// 즉, 이 JS 는 "친절한 안내", 서버 세션 도장은 "철문 자물쇠" 의 역할 분담.
function checkVerify() {
    let isVerified = document.querySelector("#isEmailVerified").value;
    if (isVerified !== "true") {
        alert("이메일 인증을 먼저 완료해주세요.");
        return false;
    }
    return true;
}
```

**강의 포인트**

- 주석은 단지 설명이 아니라 "후속 개발자가 코드를 위협하지 않도록 의도를 보존" 하는 역할.

---

## Part 3 시연 체크리스트

학생들의 "왜 서버 검증이 필요한가" 학습 효과를 최대화하려면 다음 4가지 시연을 차례로 진행하세요.

### 시연 1. 인증 안 누르고 가입 시도

```
이메일 입력만 하고 "인증번호 전송" 안 누른 채 → "회원가입" 클릭
   ↓ JS 가 막음
alert: "이메일 인증을 먼저 완료해주세요"
```

정상. JS 가 친절히 안내.

### 시연 2. 개발자 도구로 hidden 필드 변조

```
F12 → Elements 탭 → <input id="isEmailVerified"> 찾기
   → value="false" 를 value="true" 로 더블 클릭 수정
→ "회원가입" 클릭
   ↓ JS 통과 (속음)
   ↓ 서버 도착
   ↓ UserService.회원가입 → session 의 verified_email 확인 → 없음
alert: "이메일 인증을 먼저 완료해주세요"
```

서버가 막음. **수정 전 코드라면 이 시점에서 가입이 그냥 통과되어 버렸을 것.** 학생들에게 가장 강한 인상을 주는 시연.

### 시연 3. A 이메일로 인증 후 B 이메일로 가입 시도

```
"a@test.com" 으로 인증번호 받고 확인 → verified_email = "a@test.com" 세션 저장됨
→ 이메일 입력란을 "b@test.com" 으로 변경 (개발자 도구로 readOnly 해제 트릭)
→ "회원가입" 클릭
   ↓ 서버: verified_email("a@test.com") != joinDTO.email("b@test.com")
alert: "이메일 인증을 먼저 완료해주세요"
```

서버가 막음. 도장 이메일과 가입 이메일이 다르면 거부.

### 시연 4. 한 번 가입 후 동일 도장으로 또 가입

```
"a@test.com" 으로 정상 가입 완료 → 가입 직후 verified_email 도장 제거됨
→ 같은 브라우저에서 "/join-form" 다시 진입 → "a@test.com" 으로 또 가입 시도
→ "회원가입" 클릭
   ↓ 서버: 도장 없음
alert: "이메일 인증을 먼저 완료해주세요"
```

서버가 막음. 도장은 일회용이라 또 인증해야 함.

---

## 자주 묻는 학생 질문 미리 답

**Q1. JS 검증과 서버 검증, 둘 다 필요한가요?**

A. 둘 다 두는 게 모범 답안.

- JS 는 정상 사용자 실수 방지(UX). 페이지 새로고침 없이 빠른 안내.
- 서버는 진짜 자물쇠. 우회 시도 차단.

JS 만 두면 보안 구멍, 서버만 두면 사용자 경험 떨어짐.

**Q2. 세션 만료되면 어떻게 되나요?**

A. "verified_email" 도장도 같이 사라짐. 사용자가 회원가입을 하려면 이메일 인증부터 다시 시작. 자연스러운 동작.

**Q3. Service 에 HttpSession 주입은 좋은 코드인가요?**

A. 학습 단계에서는 단순화를 위해 OK. 실무에서는 Controller 에서 세션 데이터를 꺼내 메서드 인자로 전달하거나, Redis 같은 별도 저장소를 사용. 마지막 단원에서 리팩토링 예정.

**Q4. 한 사람이 인증하고 다른 사람이 가입할 수 있나요?**

A. 세션은 브라우저 단위로 분리. A 가 자기 PC 에서 인증하고 B 가 자기 PC 에서 가입 시도해도, 세션이 다르므로 도장이 안 보임. 자동 격리됨.

**Q5. 비밀번호 재설정, 결제 직전 본인 확인 등에도 같은 패턴이 가능한가요?**

A. 네. "본인 확인이 끝났다" 라는 사실을 서버가 기억해야 하는 모든 상황에 동일 패턴 적용 가능. 키 이름만 바꿔서: "verified_for_password_reset", "verified_for_payment" 등.

---

## 변경 파일 요약

### Part 1 — 기본 기능 (신규 또는 수정)

| 파일 | 내용 |
| --- | --- |
| build.gradle | spring-boot-starter-mail 의존성 추가 |
| application-dev.yml | spring.mail SMTP 설정 추가 |
| _core/util/MailUtils.java | 6자리 랜덤 코드 생성 유틸 (신규) |
| user/UserRequest.java | EmailCheckDTO 내부 클래스 추가 |
| user/MailService.java | 인증번호발송 + 인증번호확인 (신규) |
| user/UserApiController.java | /api/email/send, /api/email/verify (신규) |
| user/join-form.mustache | 이메일 인증 UI + AJAX 추가 |

### Part 2 — 중복 방지

| 파일 | 내용 |
| --- | --- |
| user/UserRepository.java | findByEmail 메서드 추가 |
| user/User.java | email 필드에 @Column(unique = true) 추가 |
| user/UserService.java | 회원가입에 이메일 중복 체크 추가 |
| _core/errors/GlobalExceptionHandler.java | Exception400 alert 처리 + DataIntegrityViolation 친절 메시지 분기 |

### Part 3 — 우회 차단

| 파일 | 내용 |
| --- | --- |
| user/MailService.java | 인증 성공 시 verified_email 도장 추가 |
| user/UserService.java | HttpSession 주입 + 회원가입 시 도장 검사 + 가입 후 도장 제거 |
| user/join-form.mustache | checkVerify 함수 주석 보강 (UX 용임을 명시) |

---

## 한 줄 마무리

> **"클라이언트는 친절한 안내, 서버는 진짜 자물쇠."**
>
> 검증은 반드시 서버에서, 인증된 사실은 세션에 도장으로 남기고, 한 번 쓰면 제거한다.

이 한 줄을 학생이 끝까지 가지고 가도록 강의 마지막에 한 번 더 강조해주세요.
