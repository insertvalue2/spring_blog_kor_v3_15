# 강의 진행 가이드 — 포인트 시스템 + 유료 게시글

이 문서는 강의를 진행하면서 그대로 따라갈 수 있도록 순서대로 정리한 가이드입니다. 각 단계마다 "목적 → 파일 → 코드 → 강의 포인트" 구조입니다.

> 원본: spring_blog_17_2026 (패키지 org.example.demo_ssr_v1_1, ID 타입 Long)
> 적용: spring_blog_kor_v3 (패키지 com.tenco.blog, ID 타입 **Integer**)
> 두 프로젝트의 타입/패키지/예외 경로가 달라 그대로 복붙이 아니라 kor_v3 구조에 맞춰 포팅했습니다.

---

## 전체 흐름 한눈에

| Part | 단계 | 목표 |
| --- | --- | --- |
| Part 1 | 1~2 | 엔티티에 포인트/유료 필드 추가 |
| Part 2 | 3~4 | 구매 도메인(purchase) 신설 |
| Part 3 | 5~7 | 구매 + 포인트충전 서비스/컨트롤러 |
| Part 4 | 8~9 | DTO + 화면(블러/구매/충전) |

핵심 개념을 한 줄로:

> 유료 게시글은 **본문을 가려두고**, 포인트로 구매한 사람(또는 작성자) 에게만 본문을 보여준다. 구매는 **포인트 차감 + 구매 내역 저장**을 하나의 트랜잭션으로 처리한다.

---

## Part 1 — 엔티티에 필드 추가

### 1단계. User 에 point 추가

**파일**: "user/User.java"

**코드** (필드 + 빌더 파라미터 + 충전/차감 메서드)

```java
// 보유 포인트 (유료 게시글 구매에 사용) — 기본값 0
@ColumnDefault("0")
private Integer point = 0;

// 빌더에 point 파라미터 추가 + null 방어
this.point = (point != null) ? point : 0;

// 포인트 차감 (구매 시). 부족하면 예외 → 트랜잭션 롤백
public void deductPoint(Integer amount) {
    if (amount == null || amount <= 0) throw new Exception400("차감할 포인트는 0보다 커야 합니다");
    if (this.point == null) this.point = 0;
    if (this.point < amount) throw new Exception400("포인트가 부족합니다. 현재 포인트: " + this.point);
    this.point -= amount;
}

// 포인트 충전 (테스트용)
public void chargePoint(Integer amount) {
    if (amount == null || amount <= 0) throw new Exception400("충전할 포인트는 0보다 커야 합니다");
    if (this.point == null) this.point = 0;
    this.point += amount;
}
```

**강의 포인트**

- 포인트 차감/충전 로직을 엔티티 안에 두는 이유: "도메인 규칙(포인트 부족 검사)" 은 엔티티가 스스로 지키게 하는 게 객체지향적.
- "@ColumnDefault("0")" 로 DB 컬럼 기본값 0. data.sql 이 point 컬럼을 명시 안 해도 0으로 들어감.

### 2단계. Board 에 premium 추가

**파일**: "board/Board.java"

**코드**

```java
// 유료 게시글 여부 (기본값 false)
// @Builder.Default: @Builder 사용 시 빌더에서 값 안 주면 필드 초기값(false) 사용
@org.hibernate.annotations.ColumnDefault("false")
@Builder.Default
private Boolean premium = false;

// update() 안에 추가
this.premium = (updateDTO.getPremium() != null) ? updateDTO.getPremium() : false;
```

**강의 포인트**

- kor_v3 의 Board 는 "@Builder + @AllArgsConstructor" 구조라 17_2026 처럼 커스텀 생성자가 없다. 그래서 "@Builder.Default" 로 기본값 false 를 보장한다. (이게 없으면 빌더로 만들 때 premium 이 null 로 들어감)

---

## Part 2 — 구매 도메인 신설 (purchase 패키지)

### 3단계. Purchase 엔티티 + Repository

**파일**: "purchase/Purchase.java" (신규), "purchase/PurchaseRepository.java" (신규)

**Purchase 핵심**

```java
@Table(name = "purchase_tb",
       uniqueConstraints = {
           @UniqueConstraint(name = "uk_user_board", columnNames = {"user_id", "board_id"})
       })
@Entity
public class Purchase {
    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Integer id;

    @ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "user_id")
    private User user;

    @ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "board_id")
    private Board board;

    private Integer price;
    // createdAt + @Builder
}
```

**Repository 핵심** (두 메서드 모두 @Query JPQL 사용)

```java
// 1) 구매 내역 객체 조회 — 구매 가격/시각 등 객체 자체가 필요할 때 (상세/환불용)
@Query("SELECT p FROM Purchase p WHERE p.user.id = :userId AND p.board.id = :boardId")
Optional<Purchase> findByUserIdAndBoardId(@Param("userId") Integer userId, @Param("boardId") Integer boardId);

// 2) 구매 여부 확인 — COUNT > 0 JPQL 로 boolean 만 받아옴 (엔티티 전체 로드 안 함 → 가벼움)
@Query("SELECT COUNT(p) > 0 FROM Purchase p WHERE p.user.id = :userId AND p.board.id = :boardId")
boolean existsByUserIdAndBoardId(@Param("userId") Integer userId, @Param("boardId") Integer boardId);
```

> 두 메서드 관계: exists 는 "샀나?" boolean 만, find 는 "구매 객체 자체" 를 돌려준다.
> 지금 단계(포인트 구매) 는 exists 만 쓰이고, find 는 결제/환불/영수증 같은 기능을 추가할 때 쓰인다.
> exists 를 default 메서드("find().isPresent()") 로 두지 않고 별도 COUNT 쿼리로 둔 이유: 존재 확인만 할 때 Purchase 엔티티 전체를 메모리에 로드할 필요가 없기 때문.

**강의 포인트**

- User 와 Board 의 N:M 관계를 Purchase 중간 테이블로 표현. (한 사람이 여러 글 구매, 한 글을 여러 사람이 구매)
- 유니크 제약 (user_id, board_id): 같은 사람이 같은 글을 두 번 구매 못 하게 DB 레벨 방어.

### 4단계. PurchaseService (구매 로직)

**파일**: "purchase/PurchaseService.java" (신규)

**핵심 흐름**

```java
@Transactional
public User 구매하기(Integer boardId, Integer userId) {   // 반환 타입 User (세션 동기화용)
    Board board = boardRepository.findById(boardId).orElseThrow(...);

    if (board.getPremium() == null || !board.getPremium()) throw new Exception400("유료 게시글이 아닙니다");

    // 작성자 본인 구매 방지 (Board.isOwner 는 예외를 던지는 구조라 직접 비교)
    if (board.getUser() != null && board.getUser().getId().equals(userId))
        throw new Exception403("자신이 작성한 게시글은 구매할 수 없습니다");

    if (purchaseRepository.existsByUserIdAndBoardId(userId, boardId)) throw new Exception400("이미 구매한 게시글입니다");

    User user = userRepository.findById(userId).orElseThrow(...);
    user.deductPoint(PREMIUM_BOARD_PRICE);   // 포인트 부족 시 여기서 예외 → 롤백

    purchaseRepository.save(Purchase.builder().user(user).board(board).price(PREMIUM_BOARD_PRICE).build());

    // 차감된 사용자 반환 → Controller 가 세션의 sessionUser 를 갱신해야
    // 마이페이지에서 차감된 포인트가 보인다. (이 return 이 없으면 DB 만 차감되고 화면은 옛 값)
    return userRepository.save(user);
}
```

**강의 포인트**

- "@Transactional" 이 핵심: 포인트 차감과 구매 내역 저장이 한 묶음. 중간에 실패하면 둘 다 롤백되어 "돈만 빠지고 구매는 안 됨" 같은 사고를 막는다.
- 4중 방어: 유료 아님 / 본인 글 / 이미 구매 / 포인트 부족.
- 17_2026 은 "board.isOwner(userId)" 로 본인 확인했지만, kor_v3 의 isOwner 는 권한 없을 때 예외를 던지는 구조라 여기서는 "직접 비교" 로 포팅했다. (구조 차이 주의 포인트)

---

## Part 3 — 서비스/컨트롤러 연결

### 5단계. BoardService — 구매 여부 포함 상세조회

**파일**: "board/BoardService.java"

**코드**

```java
private final PurchaseService purchaseService;   // 주입 추가

// 구매 여부까지 포함하는 오버로드 메서드
public BoardResponse.DetailDTO 게시글상세조회(Integer id, Integer sessionUserId) {
    Board boardEntity = boardRepository.findByIdJoinUser(id).orElseThrow(...);
    boolean purchased = purchaseService.구매여부확인(sessionUserId, id);
    return new BoardResponse.DetailDTO(boardEntity, purchased);
}
```

**강의 포인트**

- 기존 "게시글상세조회(Integer id)" 는 그대로 두고, 구매 여부가 필요한 상세보기용 오버로드를 추가. (수정폼용 인가처리는 구매 여부가 필요 없으므로 기존 것 사용)
- 순환참조 주의: BoardService → PurchaseService → (BoardRepository/UserRepository/PurchaseRepository). PurchaseService 는 BoardService 를 의존하지 않으므로 사이클 없음.

### 6단계. UserService — 포인트 충전

**파일**: "user/UserService.java"

```java
@Transactional
public User 포인트충전(Integer userId, Integer amount) {
    User user = userRepository.findById(userId).orElseThrow(...);
    user.chargePoint(amount);
    return userRepository.save(user);
}
```

### 7단계. Controller 2곳

**BoardController** — 구매 API + 상세 흐름에 canRead 계산

```java
private final PurchaseService purchaseService;   // 주입 추가

@GetMapping("/board/{id}")
public String detailPage(@PathVariable Integer id, Model model, HttpSession session) {
    User sessionUser = (User) session.getAttribute("sessionUser");
    Integer sessionUserId = sessionUser != null ? sessionUser.getId() : null;

    BoardResponse.DetailDTO detailDTO = boardService.게시글상세조회(id, sessionUserId);
    boolean isOwner = detailDTO.checkIsOwner(sessionUserId);

    // 본문 열람 가능 = 무료글이거나 / 구매했거나 / 본인 글
    // Mustache 는 AND/OR 를 못 하므로 자바에서 미리 계산
    boolean canRead = !detailDTO.getPremium() || detailDTO.getPurchased() || isOwner;

    model.addAttribute("board", detailDTO);
    model.addAttribute("checkIsOwner", isOwner);
    model.addAttribute("canRead", canRead);
    // ... replyList ...
    return "board/detail";
}

@PostMapping("/board/{id}/purchase")
public String purchaseProc(@PathVariable Integer id, HttpSession session) {
    User sessionUser = (User) session.getAttribute("sessionUser");

    // 구매 처리 → 포인트가 차감된 최신 User 를 받는다
    User updatedUser = purchaseService.구매하기(id, sessionUser.getId());

    // [중요] 세션 동기화 — 이 줄이 없으면 DB 는 차감돼도 화면 포인트는 옛 값
    session.setAttribute("sessionUser", updatedUser);

    return "redirect:/board/" + id;   // 구매 후 다시 상세 (이제 본문 보임)
}
```

**UserApiController** — 포인트 충전 API

```java
private final UserService userService;   // 주입 추가

@PostMapping("/api/point/charge")
public ResponseEntity<?> 포인트충전(@RequestBody UserRequest.PointChargeDTO reqDTO, HttpSession session) {
    reqDTO.validate();
    User sessionUser = (User) session.getAttribute("sessionUser");
    if (sessionUser == null) return ResponseEntity.status(401).body(Map.of("message", "로그인이 필요합니다."));

    User updatedUser = userService.포인트충전(sessionUser.getId(), reqDTO.getAmount());
    session.setAttribute("sessionUser", updatedUser);   // 세션 포인트 동기화
    return ResponseEntity.ok().body(Map.of("message", "포인트가 충전되었습니다.", "point", updatedUser.getPoint()));
}
```

**강의 포인트**

- "canRead" 를 자바에서 계산해 내려주는 이유: Mustache 는 "프리미엄이 아니거나 OR 구매했거나 OR 본인" 같은 복합 논리를 못 한다. 그래서 서버에서 boolean 하나로 만들어 전달.
- 구매 후 "redirect:/board/{id}" 로 다시 상세 진입하면 이번엔 구매 내역이 있으므로 본문이 열린다.
- 구매 엔드포인트는 LoginInterceptor 가 보호("/board/**" 보호, 제외는 "/board/{id:\d+}" 상세보기 GET 뿐) → 비로그인 POST 차단.
- **[중요/자주 발생하는 버그] 세션 동기화**: 구매하면 DB 의 user.point 는 차감되지만, 마이페이지는 "세션의 sessionUser" 의 포인트를 보여준다. 구매 후 "session.setAttribute("sessionUser", updatedUser)" 로 세션을 갱신하지 않으면 "DB 는 차감됐는데 화면 포인트는 그대로" 인 현상이 발생한다. 그래서 구매하기가 차감된 User 를 반환하고 Controller 가 세션을 교체한다. (포인트 충전 API 도 같은 이유로 세션 갱신함)
  - 비유: 통장(DB)에서는 돈이 빠졌는데 지갑 속 옛 잔액 메모지(세션)를 안 바꿔서 잔액이 그대로인 줄 아는 상황.

---

## Part 4 — DTO + 화면

### 8단계. DTO 3곳

| 파일 | 변경 |
| --- | --- |
| board/BoardRequest.java | SaveDTO/UpdateDTO 에 "premium" 필드 + toEntity 에 premium 반영 |
| board/BoardResponse.java | DetailDTO 에 "premium", "purchased" 필드 + (board, purchased) 생성자 |
| user/UserRequest.java | "PointChargeDTO" (amount) 추가 |

### 9단계. 화면 4곳

**save-form / update-form** — 유료 체크박스

```html
<div class="mb-3 form-check">
    <input type="checkbox" name="premium" value="true" class="form-check-input" id="premium"
           {{#board.premium}}checked{{/board.premium}}>  <!-- update-form 에만 checked 부분 -->
    <label class="form-check-label" for="premium">유료 게시글로 등록 (열람 시 500포인트 차감)</label>
</div>
```

> 체크박스 트릭: 체크 시 "premium=true" 전송, 미체크 시 아무것도 안 보냄 → 서버에서 null=false 처리.

**detail.mustache** — 유료 배지 + 블러/구매

```html
{{#board.premium}}<span class="badge bg-warning text-dark">유료</span>{{/board.premium}}

{{#canRead}}
    <div class="m-4 p-2">{{board.content}}</div>
{{/canRead}}
{{^canRead}}
    <div class="...">
        유료 게시글입니다. 구매 후 열람 가능합니다. (500 포인트)
        {{#sessionUser}}
            <form action="/board/{{board.id}}/purchase" method="post" onsubmit="return confirm('구매하시겠습니까?')">
                <button type="submit" class="btn btn-primary">구매하기 (500P)</button>
            </form>
        {{/sessionUser}}
        {{^sessionUser}}<a href="/login-form" class="btn btn-outline-primary">로그인 후 구매</a>{{/sessionUser}}
    </div>
{{/canRead}}
```

**user/detail.mustache** — 포인트 표시 + 충전 AJAX

```html
<span id="pointDisplay">{{user.point}}</span> P
<input type="number" id="chargeAmount" value="1000">
<button onclick="callChargeApi()">포인트 충전</button>
```
```javascript
async function callChargeApi() {
    let amount = Number(document.querySelector("#chargeAmount").value);
    let response = await fetch("/api/point/charge", {
        method: "POST", headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ amount: amount })
    });
    let result = await response.json();
    if (response.ok) document.querySelector("#pointDisplay").innerText = result.point;
}
```

---

## 테스트 시나리오 (강의 시연용)

```
0) 앱 재시작 (ddl-auto: create → 테이블 새로 생성, data.sql 재실행)
1) ssar 로 로그인 → 마이페이지 → 포인트 0 확인 → 1000 충전 → 1000P 표시
2) cos 로 로그인 → 글쓰기 → "유료 게시글" 체크 → 등록
3) ssar 로 로그인 → cos 의 유료 글 상세 진입
   → 본문 안 보이고 "구매하기 (500P)" 버튼만 보임
4) 구매하기 클릭 → 포인트 500 차감 → 본문 열림
5) 같은 글 다시 들어가면 → 이미 구매 상태라 본문 바로 보임
6) cos 로 로그인 → 자신의 유료 글 → 본문 바로 보임 (작성자는 구매 불필요)
```

**경계 케이스 확인**

| 상황 | 기대 결과 |
| --- | --- |
| 포인트 부족 상태로 구매 | "포인트가 부족합니다" alert |
| 이미 구매한 글 또 구매 (URL 직접) | "이미 구매한 게시글입니다" alert |
| 본인 유료 글 구매 시도 (URL 직접) | "자신이 작성한 게시글은 구매할 수 없습니다" alert |
| 비로그인으로 구매 POST | LoginInterceptor 가 로그인 화면으로 |

---

## 17_2026 → kor_v3 포팅 시 바뀐 점 (주의)

| 항목 | 17_2026 | kor_v3 (적용본) |
| --- | --- | --- |
| ID 타입 | Long | **Integer** |
| 패키지 | org.example.demo_ssr_v1_1 | com.tenco.blog |
| 예외 경로 | _core.errors.exception.Exception400 | _core.errors.Exception400 |
| Board 생성 방식 | 커스텀 생성자 (premium null 방어) | @Builder + @Builder.Default |
| 본인 글 확인 | board.isOwner(userId) (boolean 반환) | 직접 비교 (kor_v3 isOwner 는 예외 던짐) |
| 상세 본문 표시 | DetailDTO 의 isPurchased 등 조합 | Controller 에서 canRead 계산 후 전달 |

---

## 변경 파일 요약

**신규 (3)**: purchase/Purchase.java, purchase/PurchaseRepository.java, purchase/PurchaseService.java

**수정 (10)**:
- user/User.java (point + 충전/차감)
- board/Board.java (premium)
- board/BoardRequest.java (premium)
- board/BoardResponse.java (premium, purchased)
- user/UserRequest.java (PointChargeDTO)
- board/BoardService.java (구매여부 포함 상세조회)
- user/UserService.java (포인트충전)
- board/BoardController.java (구매 API + canRead)
- user/UserApiController.java (포인트 충전 API)
- templates: board/save-form, board/update-form, board/detail, user/detail

---

## 한 줄 마무리

> 유료 게시글은 **본문을 가려두고**, 포인트 구매자(또는 작성자) 에게만 연다. 구매는 **포인트 차감 + 구매 내역 저장을 한 트랜잭션**으로 묶어 정합성을 지킨다. 화면의 복합 조건(canRead) 은 Mustache 대신 서버에서 계산한다.
